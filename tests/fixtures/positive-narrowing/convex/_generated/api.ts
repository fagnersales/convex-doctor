// Stub for the fixture — real Convex codegen has the same surface.
export const internal: {
  helpers: {
    findCatalog: { _name: "helpers/findCatalog" };
    makeCatalog: { _name: "helpers/makeCatalog" };
  };
} = {} as never;
